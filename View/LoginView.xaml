<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppToDo.View.LoginView"
             xmlns:vm="clr-namespace:MauiAppToDo.ViewModel"
             Title="LoginView">
    <Grid Margin="20" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Connexion" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">

            <TextBlock Text="Nom d'utilisateur" />
            <TextBox Text="{Binding UserName}" x:Name="UserName" Width="250" Margin="0,5,0,10" Padding="5"/>

            <TextBlock Text="Mot de passe" />
            <TextBox Text="{Binding Password}" x:Name="Password" Width="250" Margin="0,5,0,10" Padding="5"/>
        </StackPanel>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Grid.Row="2" Margin="0,10,0,0" HorizontalAlignment="Center"/>

        <Button Grid.Row="3" Content="Se connecter" Width="120" Height="30" 
	            HorizontalAlignment="center"
	            Command="{Binding LoginCommand}"/>
    </Grid>
</ContentPage>